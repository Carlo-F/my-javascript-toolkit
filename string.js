let s = 'Hello, World!';

s.substring(1,4)
s.slice(1,4)
s.split(",")

s.indexOf("1")
s.lastIndexOf("1")

s.startsWith("Hell") // true
s.EndsWith("!") // true
s.include("not") // false
s.replace("llo", "ya")

s.toLowerCase()
s.toUpperCase()

s.cartAt(0)

s.trim()

s.concat("!")
